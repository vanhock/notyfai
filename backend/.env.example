SUPABASE_URL=https://your-project.supabase.co
# Publishable API key — used for user-scoped API calls so RLS applies. Required.
SUPABASE_PUBLISHABLE_KEY=your-publishable-key
# Secret key (Dashboard → Secret keys) — used only for webhook handler (no user JWT). Keep secret.
SUPABASE_SECRET_KEY=your-secret-key
HOOK_SECRET=your-random-secret-for-signing-hook-tokens
PORT=3000
# Base URL for hook links (use localhost for local dev; set to your API URL in production)
BASE_URL=http://localhost:3000
